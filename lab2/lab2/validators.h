#pragma once

// With this function we validate the product's name
int validateProductName(char name[]);

// With this function we validate the product's quantity
int validateProductQuantity(char quantity[]);

// With this function we validate the product's type
int validateProductType(char type[]);

// With this function we validate the product's type
int validateExpirationDate(char date[]);

